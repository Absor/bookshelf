require 'spec_helper'

describe "Shelves" do
  describe "GET /shelves" do
    it "works! (now write some real specs)" do
      get shelves_path
      expect(response.status).to be(200)
    end
  end
end
